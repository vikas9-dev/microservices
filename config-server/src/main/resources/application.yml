server:
  port: 8071

spring:
  application:
    name: config-server

  profiles:
    active: native # classpath

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
